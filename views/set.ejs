<!DOCTYPE html>
<html>

<head>
  <title><%= title || "Lego Set Details" %></title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/theme.css">
</head>

<body>
  <%- include('partials/navbar', { page: '' }) %>

  <br />

  <div class="container">
    <main class="col-md-12">
      <section class="bg-light text-center py-5">
        <h1 class="display-5 fw-bold"><%= set.name %></h1>
        <p class="lead text-muted">
          Below, you will find detailed information about the set: <strong><%= set.name %></strong>
        </p>

        <% if (set.img_url) { %>
          <img src="<%= set.img_url %>" alt="Image of <%= set.name %>" class="img-fluid my-3" />
        <% } %>

        <ul class="list-group mx-auto" style="max-width: 500px;">
          <li class="list-group-item"><strong>Name:</strong> <%= set.name %></li>
          <li class="list-group-item"><strong>Year Released:</strong> <%= set.year %></li>
          <li class="list-group-item"><strong>Theme:</strong> <%= set.theme || "No theme specified" %></li>
          <li class="list-group-item"><strong>Number of Parts:</strong> <%= set.num_parts %></li>
        </ul>

        <div class="mt-4">
          <a href="/lego/deleteSet/<%= set.set_num %>" class="btn btn-danger me-2">Delete Set</a>
          <a href="#" onclick="history.back(); return false;" class="btn btn-secondary">Back</a>
        </div>
      </section>
    </main>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
</body>

</html>